# DAG ledger — 3-bug investigation fan-out

Session: 01a07626-f59c-7663-ae2a-4f12dad65624
Repo: /home/acbraith/projects/screen-time @ d519345 (clean tree, committed pre-wave)
Phone-repro time budget: user asks to run a repro only if my own repro >5min
or cumulative phone repros >10min. Spent so far: **~30s** (one package-query + log reads; zero UI touches).

## Wave 1 (parallel, no deps, cap 3/6)

| Node | Run | Target | Status | Result |
|---|---|---|---|---|
| bug1-lockscreen | b11e050c | keyguard-blind event path trace + PC repro + fix | running | — |
| bug2-plugin-picker | a0570a32 | unlistable plugin pkg trace + PC repro + fix options | running | — |
| bug3-overlay-sticky | d9a3e172 | sticky-overlay starvation trace + PC repro + fix | running | — |

Output contract per node: ≤25 lines (ROOT-CAUSE trace, PC-REPRO, FIX, RISKS).
Rule: no production edits by children; failing-test files allowed only if they compile.

## Next (after wave 1 settles)

1. Fan-in: synthesize 3 reports into per-bug fix plans.
2. Quick phone confirmations via adb (each seconds-long; stay within budget).
3. Implement fixes (TDD where pure), full build, install, user acceptance.

## Wave 1 results (all completed)

- bug1-lockscreen: keyguard-blind event path confirmed; fix = early return in
  onAccessibilityEvent + onScreenOn guard + launchBlock guard.
- bug2-plugin-picker: plugin unlistable confirmed via adb (301 launcher
  activities, no plugin); fix = survivalPackages += miui.systemui.plugin.
  PluginSurfaceReproTest RED->GREEN.
- bug3-overlay-sticky: survival overlay nulls lastGated + oracle-Skip starves
  block; fix = survival doesn't null anchor + verdict falls through to sticky
  anchor for survival roots. Bug3OverlayVerdictTest RED->GREEN.
  (One self-caught regression mid-fix: own-root must still Skip — existing
  test caught it, narrowed to survival-only.)

## Fan-in: implemented commit `Lockscreen-blind gate, plugin survival,
transparent survival overlays at deadlines` — 50/50 unit tests green, lint clean.
Next: install + single combined user acceptance round.

## Wave 2 (research: replace hardcoded overlay list) — NO implementation

| Node | Run | Target | Status | Result |
|---|---|---|---|---|
| overlay-signals | 2b84d65f | API survey: runtime overlay-vs-use signals, HyperOS reliability ranking | running | — |
| overlay-empirical | ba8ad34f | READ-ONLY adb probes (dumpsys window/appops/focus; zero UI touches) | running | — |

Constraint: user asked for plan only. Fan-in = synthesis + recommendation by parent.

## Wave 2 results (both completed)

- overlay-signals: ranked API survey. Top: usage-oracle divergence (5/5),
  getWindows()+type/layer/focus (4/5). AppOps capability, NLS, AM tasks all ~1.
- overlay-empirical (READ-ONLY dumpsys, zero UI touches, ~2min passive):
  ubktouch = 2x ty=ACCESSIBILITY_OVERLAY (2032), 1x1px, NOT_FOCUSABLE/
  NOT_TOUCHABLE, layer 311k, via CREATE_ACCESSIBILITY_OVERLAY op (SAW=ignore);
  plugin = ty=KEYGUARD_DIALOG (2009) uid-1000 invisible; apps = BASE_APPLICATION
  layer 21k; our BlockActivity also 21k; usage access confirmed allow.
