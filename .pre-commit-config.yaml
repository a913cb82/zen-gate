repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: ['--maxkb=2048']
      - id: check-merge-conflict
      - id: mixed-line-ending
        args: ['--fix=lf']
  # Kotlin lint (ktlint via Gradle) is wired here once the M1 scaffold lands.
  # Placeholder local hook — uncomment after verifying ./gradlew ktlintCheck:
  # - repo: local
  #   hooks:
  #     - id: ktlint
  #       name: ktlint
  #       entry: ./gradlew -q ktlintCheck
  #       language: system
  #       pass_filenames: false
